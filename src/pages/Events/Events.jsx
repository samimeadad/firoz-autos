import React from 'react';
import Header from '../../components/Header/Header';

const Events = () => {
    return (
        <div>
            <Header />
            <h1>Events Page</h1>
        </div>
    );
};

export default Events;